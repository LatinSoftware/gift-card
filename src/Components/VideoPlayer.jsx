import { useEffect, useState } from "react"

const VideoPlayer = () => {

Effect(() => {
        if(!showGift && playerState.progress >= 40)
          setShowGift(true);
    
      }, [playerState.progress])
    
}

export {VideoPlayer}